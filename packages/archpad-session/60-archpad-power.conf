[Login]
# Power-key actions stay inert until the PAM-backed session lock and shell
# power menu are installed. This avoids both accidental shutdown and resuming
# directly into an unlocked graphical session.
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
