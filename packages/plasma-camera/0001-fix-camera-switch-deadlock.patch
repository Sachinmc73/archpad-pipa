--- plasma-camera-26.08.0.orig/src/plasmacamera.cpp	2026-08-12 03:15:47.000000000 +0530
+++ plasma-camera-26.08.0/src/plasmacamera.cpp	2026-09-10 20:18:14.778732591 +0530
@@ -313,6 +313,15 @@ void PlasmaCamera::setCameraDevice(const
         return;
     }
 
+    if (m_state == State::Running && m_camera) {
+        qDebug() << "Stopping active camera before switching to" << cameraDeviceId;
+        setState(State::Stopping);
+        m_camera.reset();
+        m_cameraId = cameraDeviceId;
+        QMetaObject::invokeMethod(m_cameraWorker, "unsetCamera", Qt::BlockingQueuedConnection);
+        return;
+    }
+
     qDebug() << "Using camera: " << cameraDeviceId;
     m_cameraId = cameraDeviceId;
 
